services:
  db:
    image: postgres:16.11
    env_file:
      - ./.env/.db
    volumes:
      - finanzas_dev:/var/lib/postgresql/data
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - ./.env
    ports:
      - "8000:8000"
    depends_on:
      - db
